import 'package:flutter/material.dart';

class MobileUnsupportedView extends StatelessWidget {
  const MobileUnsupportedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}